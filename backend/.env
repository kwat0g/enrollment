# Backend Environment Variables
# Copy this file to .env and set the values accordingly.

# MySQL database configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ncst_enrollment1
DB_PORT=3306

# JWT configuration
# Use a strong, random secret in production
JWT_SECRET=kwatog
# Token expiry (e.g., 1d, 12h, 60m)
JWT_EXPIRES_IN=60m

# Server port
PORT=5000

# Node environment
environment=development
